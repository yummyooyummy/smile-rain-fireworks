# 给 Claude Design 的提示词（整段复制，一次发完）

> 下面这段是最终决策版：雨与烟花互斥、手动钮只在无摄像头时出现、参数面板藏在设置键长按里、
> 没有独立错误页、没有跳过按钮。代码会按这份规格改，所以图和代码不会打架。

---

```
你是一名资深交互/视觉设计师。请为一个浏览器端 AR 互动 Demo 出一整套高保真界面，
以手机竖屏 390×844 为主、桌面 1440×900 为辅。产品叫「笑雨烟花」：
打开前置摄像头，用户微笑 → 全屏下雨；大笑 → 画面底部升起烟花在高处炸开，
烟花粒子落到用户头上会分裂溅开。这是投递 TikTok 直播「AI 产品设计师」岗位的笔试作品，
评审会在手机和电脑上真的打开来玩，所以界面要经得起实际使用，不是概念稿。

════════ 一、视觉世界（严格遵守） ════════

单一暗调暖色主题，永远叠在摄像头画面上，不做浅色版。只允许以下颜色，不得引入新色相：
  文字 #F5EDE6　次要文字 #A4948A　底色 #14100F　面板底 #1E1815
  描边 rgba(245,237,230,0.14)　主色（珊瑚）#F0785A　金 #E9B95A
  雨色 #BFD4F2　烟花四色 #F0785A / #E9B95A / #FFF3E0 / #6FC3B8
玻璃容器：rgba(20,16,15,0.6) + 背景模糊 8–10px + 1px 描边，圆角 999px（胶囊）或 16px（面板）。
主按钮：珊瑚→金 135° 渐变胶囊。幽灵按钮：透明底 + 描边。
字体：系统中文无衬线（PingFang SC / Noto Sans SC），数字用等宽数字。
所有可点元素 ≥ 40×40px；所有浮层留出刘海与底部安全区。
摄像头画面用「暗色渐变 + 一个居中偏上的浅色人形剪影」占位，不要放真人照片；
剪影所在的竖直中间 60% 区域是人脸区，任何 UI 不得进入，这是硬约束。
粒子（雨丝、烟花）请画出示意：雨是三层景深的细线，远层淡而慢、近层亮而粗；
烟花是从底部升起的一道尾迹在上方炸开的辐射粒子，碎片落到剪影头顶处有细小的溅开。

════════ 二、页面与状态 ════════

【S1 开始页】背景不是摄像头，是暗色暖调渐变。垂直居中一列：
  - 可选的玩法预览视频槽位（9:16，圆角 18，最高 34vh）。请出两版：有槽位、无槽位，
    因为视频文件可能不存在，布局在没有它时必须同样成立。
  - 标题「笑一下，屏幕会开花」
  - 副文案「微笑会下雨，大笑会放烟花。需要打开摄像头。」
  - 主按钮「开始」
  - 底部一行小字「Live Demo · 画面只在本机处理，不会上传」
  按钮的四种状态（各出一版）：
  a. 默认「开始」
  b. 请求权限中：按钮内左侧一个旋转指示器 + 文案「正在打开摄像头」，按钮禁用。
     不要进度条——这一步时长未知。
  c. 加载模型中：按钮内旋转指示器 + 文案「正在准备」；按钮正下方一行 12px 次要色文字
     「正在加载识别模型 63%」。只有这一行字，没有进度条。
  d. 摄像头没拿到（拒绝 / 超时 / 不支持）：主按钮文案变为「摄像头未开启，直接开始」，
     按钮下方两行：一行次要色的开启路径（示例文案：「点地址栏左边的「ᴀA」→ 网站设置 →
     摄像头 → 允许」），一行文字链接「重试摄像头」。没有独立的错误页，用户始终留在这一页。

【S2 主画面 · 摄像头模式】摄像头铺满（镜像），粒子层在上，UI 在最上。
  右上角一组三个 40px 玻璃圆钮，间距 8px，从左到右：隐藏 UI（眼睛）、设置（三条滑杆）、
  退出（×）。退出直接回到 S1，不弹确认。
  顶部居中一个临时状态胶囊（只在需要时出现）：「把脸放进画面里」「光线有点暗，换个亮一点的地方」。
  底部居中是引导区：一行引导文字 + 其下**两根**细进度条上下叠放，每根左侧带 11px 标签：
    「微笑」条填充色 #BFD4F2　「大笑」条填充色 #F0785A，条宽 160px、高 3px、圆角。
  引导文字按步骤切换：「对着镜头微笑，雨就会落下来」→「再张开嘴大笑试试」→
  「摆摆头，让烟花撞到你」→「玩法解锁了，尽情笑吧」；走完后文字淡出，两根条常驻。
  右下角一个 48px 玻璃圆钮「分享截图」（向上箭头出托盘图标）。
  没有手动触发按钮，没有跳过按钮。
  雨与烟花**互斥**：微笑只有雨；判定为大笑后雨淡出、只剩烟花；回到微笑雨再淡回来。
  请出这几版：
  1. Idle：无粒子，引导第一句，两根条都是空的
  2. Smiling：三层雨，微笑条满、大笑条约 1/3，引导第二句
  3. Laughing：没有雨、只有烟花，两根条都满，引导第三句，剪影头顶有粒子溅开
  4. 引导走完：只剩两根条 + 右上三钮 + 右下分享钮
  5. 隐藏 UI 态：所有 UI 消失，只在右上角原位留一个 24px、30% 透明度的圆点，点它恢复
  6. 分享成功 toast：底部引导区上方一个胶囊「截图已保存」
  7. 空闲暂停：全屏 72% 暗色遮罩 + 模糊，居中「一直没看到人，已经暂停了」/ 次要色「点一下继续」

【S3 主画面 · 手动模式（没有摄像头时）】背景是 S1 那种暗色渐变，粒子照常。
  顶部居中一条胶囊横幅：左侧金色 6px 圆点 +「手动模式 · 表情识别未开启」+ 右侧小幽灵钮
  「重新连接摄像头」。
  右下角改为 56px 主色圆钮（中心一颗珊瑚→金渐变的 18px 圆点），是这个模式下唯一的玩法入口；
  钮左侧一行次要色小字「点一下下雨 · 长按放烟花」。长按时圆钮外圈出现一圈环形进度。
  右上角三钮保留。没有引导文字、没有进度条、没有分享钮。
  请出两版：静止；长按中（环形进度走到 2/3，底部有一枚烟花正在升起）。

【S4 设置面板】短按设置键打开基础版；**长按设置键（≥600ms）**打开的是同一个面板但展开了
  「高级」区。手机是底部抽屉（顶部一条 36×4px 拖拽把手，最高不超过屏幕竖直中点，
  内容多了内部滚动）；桌面是右侧 320px 侧栏。面板不做全屏遮罩，画面中央始终可见。
  头部：标题「设置」+ 右上 ×。
  基础区：
    - 开关「显示提示与进度条」
    - 幽灵按钮「重看新手引导」
  高级区（标题「触发阈值」，标题下一行次要色说明「给运营 / 设计调参用，改完不用重新部署」）：
    - 六个滑杆，每个右侧显示当前值：微笑阈值 0.45 / 大笑阈值 0.35 / 雨量上限 1.00 /
      烟花粒子数 180 / 碰撞弹性 0.55 / 色调偏移 0
    - 开关「显示碰撞体」「显示调试数据」
    - 主按钮「复制配置给开发」，点击后按钮下方出现金色小字「已复制到剪贴板」
  请出：手机基础版、手机高级展开版、桌面高级展开版。

════════ 三、输出清单 ════════

手机 390×844（共 15 块画板）：
  S1 默认（无预览槽位）、S1 默认（有预览槽位）、S1 请求权限中、S1 加载模型中、S1 摄像头未开启、
  S2 Idle、S2 Smiling、S2 Laughing、S2 引导走完、S2 隐藏 UI、S2 分享 toast、S2 空闲暂停、
  S3 静止、S3 长按中、S4 基础版、S4 高级展开
桌面 1440×900（共 3 块）：S1 默认、S2 Smiling、S4 高级展开（右侧栏）
另加一块「组件表」：三种按钮的默认/按下/禁用态、玻璃胶囊、两根进度条、开关、滑杆、toast。

════════ 四、不要做的事 ════════

不要浅色主题；不要新的色相；不要在开始页画进度条；不要独立的错误页；
不要「跳过」按钮；摄像头模式下不要手动触发钮；UI 不进入人脸区；
不要在面板里显示「当前档位」之类用户看不懂的信息；不要给退出加二次确认。
```
